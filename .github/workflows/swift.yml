# This workflow will build a Swift project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-swift

name: Swift

on:
  push:
    branches: [ "main", "develop" ]
  pull_request:
    branches: [ "main", "develop" ]

jobs:
  build:
  
    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v3

    - name: Build
      run: xcodebuild build -project GoInvest.xcworkspace -scheme GoInvest clean build -sdk iphoneos CODE_SIGNING_ALLOWED=No

    # - name: Build
    #   run: |
    #         xcodebuild clean build -workspace "GoInvest.xcworkspace" -scheme "GoInvest" -destination "platform=iOS Simulator,name=iPhone 15 Pro,OS=latest" CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO ONLY_ACTIVE_ARCH=NO
    # - name: Test
    #   run: |
    #        xcodebuild clean test -workspace "GoInvest/GoInvest.xcworkspace" -scheme "GoInvest" -destination "platform=iOS Simulator,name=iPhone 15 Pro,OS=latest" CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO ONLY_ACTIVE_ARCH=NO
